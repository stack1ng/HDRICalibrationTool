import { describe, expect, it, test } from "vitest";
import { onDrop } from "./onDrop";

describe("onDrop", () => {
	it("should accept a directory as an input", async () => {});

	it("should accept a file as an input", async () => {});

	it("should accept a directory and a file as an input", async () => {});
});
